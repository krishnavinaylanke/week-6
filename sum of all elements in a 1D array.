#include <stdio.h>
 
int main() {
  	int a[100], n, i, sum = 0;
  	printf("25331A05G3\n");
  	printf("Enter size of array: ");
  	scanf("%d", &n);
  	printf("Enter elements: ");
  	for (i = 0; i < n; i++)
        	scanf("%d", &a[i]);
  	for (i = 0; i < n; i++)
        	sum += a[i];
  	printf("Sum of elements = %d\n", sum);
  	return 0;
}
